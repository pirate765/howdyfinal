
$(document).ready(function () {
  $('#activitydate-1').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });

  $('#activitydate-1').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-2').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-3').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-4').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-5').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-6').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-7').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-8').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-9').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });
  $('#activitydate-10').datepicker({
    format: "dd/mm/yyyy"
  }).on('change', function () {
    $('.datepicker').hide();
  });

});
